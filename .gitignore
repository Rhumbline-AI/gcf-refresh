build
dist / media
node_modules
.DS_Store
.env
.next
.vercel

# Payload default media upload directory
public/media/

# Large video assets — should be uploaded via Payload Media or Vercel Blob, not committed
src/videos/*.mp4
*.mp4
*.mov

# Local DB backups — never commit (may contain user data + URLs)
backups/

public/robots.txt
public/sitemap*.xml


# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
.env*.local
